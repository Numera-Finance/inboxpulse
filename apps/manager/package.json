{
  "name": "@crm/manager",
  "version": "0.1.0",
  "private": true,
  "description": "InboxPulse manager dashboard API — the aggregate queries behind the extension's Dashboard, AI Analysis, Customers and Users tabs.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "postgres": "^3.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
